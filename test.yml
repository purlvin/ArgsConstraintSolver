%YAML 1.1
---
#------------------------------------------------
# Top: test.yml

templates:
  # -> Test templates {
  __math_t : &__math_t
    _constr_grps : &__math_t__constr_grps
      - global_default=1
    _when  : &__math_t__when
      [quick, nightly, math]
  # }!templates

includes:
  - suite_conv.yml



